# Managed by Ansible (roles/promtail) — do not edit by hand.
server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /var/lib/promtail/positions.yaml

clients:
  - url: http://{{ hostvars['linux-observability']['ansible_host'] }}:3100/loki/api/v1/push

scrape_configs:
  - job_name: journal
    journal:
      max_age: 12h
      path: /var/log/journal
      labels:
        job: systemd-journal
        host: "{{ inventory_hostname }}"
    relabel_configs:
      - source_labels: ["__journal__systemd_unit"]
        target_label: "unit"
