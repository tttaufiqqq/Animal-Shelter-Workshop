# Same local/dev-only APP_KEY already committed in docker-compose.yml
# (Stage 4) — not a real production secret, kept as plain stringData for
# the same reason that file does.
apiVersion: v1
kind: Secret
metadata:
  name: asw-app-secret
type: Opaque
stringData:
  APP_KEY: "base64:M0OUllc66N9E5MtY5VzOs0v29nIM+2EWbzj+z1V9+5Y="
