# Dedicated ServiceAccount for asw-app pods, bound to Vault's asw-app
# Kubernetes-auth role (secret/animal-shelter-workshop, read-only, same
# asw-deploy policy the Ansible AppRole deploy already uses).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: asw-app
